# Admin Notes (bmi.adminnotes)

The table uses:
- `created_by` – contributor name (who added/updated the comment)
- `created_on` – datestamp (date/time when the note was created; app shows date only in the UI)

No schema changes required.
